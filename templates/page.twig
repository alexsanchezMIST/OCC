{% extends "base.twig" %}

{% block content %}

{# BEGIN HERO SECTION #}
<section id="hero" class="section">
    <div class="container">
        <div class="hero__wrapper">
            <h1>{{ post.title }}</h1>
            <a href="/" class="btn--primary">Become a member →</a>
        </div>
    </div>
</section>
{# END HERO SECTION #}

{# BEGIN CONTENT SECTION #}
<section id="content" class="section">
    <div class="container">
        <article id="post-{{ post.ID }}" class="content__body post-type-{{ post.post_type }}">
            {{ post.content }}
		</article>
    </div>
</section>
{# END CONTENT SECTION #}


{% endblock %}